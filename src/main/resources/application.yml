server:
  port: 8080

spring:
  application:
    name: boardgame-service

# Swagger 설정
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
